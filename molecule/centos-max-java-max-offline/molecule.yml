---
dependency:
  name: galaxy

driver:
  name: docker

lint:
  name: yamllint

platforms:
  - name: ansible-role-java-centos-max-java-max
    image: centos:7

provisioner:
  name: ansible
  playbooks:
    converge: ../java-max-offline/playbook.yml
  lint:
    name: ansible-lint

scenario:
  name: centos-max-java-max-offline

verifier:
  name: testinfra
  directory: ../java-max/tests
  lint:
    name: flake8
