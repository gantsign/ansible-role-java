---
dependency:
  name: galaxy

driver:
  name: docker

lint:
  name: yamllint

platforms:
  - name: ansible-role-java-ubuntu-max-java-max
    image: ubuntu:18.04

provisioner:
  name: ansible
  playbooks:
    converge: ../java-max-offline/playbook.yml
  lint:
    name: ansible-lint

scenario:
  name: ubuntu-max-java-max-offline

verifier:
  name: testinfra
  directory: ../java-max/tests
  lint:
    name: flake8
